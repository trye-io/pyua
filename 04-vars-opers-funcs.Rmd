# Змінні, оператори, функції, методи та атрибути {#vars}

<div class="btn-group">
  <button class="lecture">🎥 Лекція</button>
  <button class="podcast">🎙️ Подкаст</button>
  <button class="notebook" onClick="parent.open('https://colab.research.google.com/drive/11qj9yGvFT8OU2gJwnUvveXiJwTsRWTMf?usp=sharing')">📓 Записник</button>
  <button class="exercises" onclick="openDetailsById('vars_exercises');">🤸 Вправи</button>
  <button class="cheat" disabled>📝 Шпора</button>
</div>

**⏱️ Час на опанування теми:** 30 хвилин

**🤷 Для чого ми це вивчаємо:**

- надавати імена важливим числам, щоб потім ці імена використовувати в обчисленнях
- вміти порахувати будь-що у Python
- використовувати маленькі корисні програми та не програмувати прості речі з нуля

**🔑 Результати навчання:**

- Створювати, використовувати та змінювати значення змінних
- Використовувати математичні оператори для підрахунків
- Викликати функцію та зазначати аргументи
- Отримувати інформацію про об'єкти та змінювати їх за допомогою методів та атрибутів

```{r, echo=FALSE, message=FALSE, warning=FALSE, error=TRUE}
#-------------------------------------------------------------------------------
# Code below is for knit caching purposes

library(reticulate)
library(jsquiz)
source("term.R")

# set up all messages 
set_button_label("Перевірити")
set_placeholder("Пишіть тут...")
set_success_messages(
    c(
        "Так тримати, чемпіон 🌟",
        "Просто супер 🤩",
        "Ти -- молодец 😎",
        "Йой, бачу довідченого кодера 🤓",
        "А ти точно не прцював в OpenAI до цього 🤖"
    )
)
set_failure_messages(
    c(
        "Упс, ще трошки 😬",
        "Це не зовсім те, що ми мали на увазі, але вже близько 🙊",
        "Ти ще на один крок ближче до успіху, тільки не здавайся 🚀",
        "Нічого, головне не здаватися 💪"
    )
)
```

---

Найпершим концептом в програмуванні який ми вивчимо, будуть змінні. Ми навчимося створювати нові змінні, використовувати та змінювати їх значення. Далі, ми перейдемо до математичних операторів, щоб навчитись рахувати будь-що у Python. Потім, ми познайомимось з функціями та навчимося їх викликати. В кінці цієї глави, ми розберемося з методами та атрибутами.

---

## ❎ Створюємо, використовуємо та змінюємо змінні

<div class = "warning">
🎈 **Увага**: У цій книзі код знаходиться у спеціальних секціях, які виділенні іншим ніж текст фоном. Результат виконання коду буде починатись з символів `##` і буде надрукований зразу ж під кодом.
</div>

Зараз ми почнемо будувати наші навички з програмування. Філософія наша буде проста -- ми будемо використовувати те, що ми вже знаємо, і по цеглині 🧱 додавати нові знання. Якщо ви хочете прискорити цей процес в кілька разів, наполегливо рекомендується створити новий записник Colab 📓 та кодувати паралельно із книгою. Почнемо ми зі змінних.

<div class = "warning">
🎈 **Увага**: У Python як десятковий розділювач використовується точка `.` замість коми `,`. Тобто `5.5` ми інтерпретуємо як п'ять цілих п'ять десятих. У цій книзі ми також будемо користуватись точкою `.`, щоб не заплутатись.
</div>

Скажімо, у вашому смартфоні збережено дуже велика кількість фотографій 📸 і залишилось `5.5` `r term("гігабайт", "gigabytes, скороченно Gb")` пам'яті з `64` гігабайт доступних. Спочатку, давайте з'ясуємо скільки вільного місця залишилось в `r term("мегабайтах", "megabytes, скорочено Mb")`. Нагадаємо, що в одному гігабайті міститься `1000` мегабайт. Ми вже вміємо використовувати Python як калькулятор 🧮, то ж давайте порахуємо:

```{python}
5.5 * 1000
```

Тепер нам цікаво скільки фотографій ще ми можемо зробити. Одна фотографія на вашому смартфоні займає приблизно `3` мегабайти. Нам треба перевести `5.5` гігабайти у мегабайти, тобто `5.5 * 1000` і розділити на розмір фотографії, тобто `3` мегабайти: 

```{python}
5.5 * 1000 / 3
```

А тепер ми розрахуємо який відсоток вашої загальної пам'яті залишається доступним. Для цього ділимо кількість вільної пам'яті (`5.5` гігабайт) на `64` гігабайти та множимо на 100%: 

```{python}
5.5 / 64 * 100
```

Через кілька днів ваша пам'ять зменшилась ⬇️ до `5` гігабайти та ми хочемо порахувати усе те, що ми рахували до цього: 1️⃣ приблизну кількість фотографій і 2️⃣ відсоток вільної пам'яті:

```{python}
5 * 1000 / 3
```

```{python}
5 / 64 * 100
```

Так само, через кілька днів, пам'ять зменшується ⬇️ до `4.5` і нам треба порахувати ще разочок: 

```{python}
4.5 * 1000 / 3
```

```{python}
5.5 / 64 * 100
```

Якщо ви уважно дивились 👀 на цифри, то звернули увагу на те, що відсоток вільної пам'яті збільшився ⬆️ з приблизно `7.8` до `8.6` відсотків. Як таке можливо? Якщо ми придивимось, ми побачимо що ми наплутали 🫤 та зазначили `5.5` замість `4.5`. Насправді ми зробили цю помилку не випадково. Ми використовували й переписували одне і те саме число (кількість вільної пам'яті у гігабайтів) двічі, і тому вірогідність зробити помилку хоча б в одному місці доволі велика.

Саме цей факт і є мотивацією нового концепту який називається `r term("змінна", "variable")`. Формально, **змінна** -- це **ім'я**, яке вказує на певний `r term("об'єкт", "object")` (тобто ділянку пам'яті комп'ютера).^[Ми не будемо зараз розбиратись, як саме це працює -- нам набагато важливіше зрозуміти як керувати цим процесом. Це як з автівками, нам не обов'язково розуміти як працює двигун, щоб навчитись керувати. Але ми повернемось до цієї теми у Додатку \@ref(semantics)] У цій главі під об'єктами ми будемо розуміти числові `r term("значення", "values")`. Ми можемо створювати нові змінні, змінювати їх значення, але найважливіше -- використовувати значення змінних у коді. 

Давайте створимо нову змінну з ім'ям `free_space` (з англійської -- вільне місце) і присвоїмо цій змінній значення `4.5`:

```{python}
free_space = 4.5
```

Синтаксис створення змінних простий і складається з трьох частин: **ім'я** змінної зліва, потім **знак дорівнює**, а все що залишається праворуч -- це буде **значення**.

```{r, echo=FALSE, out.width="100%"}
knitr::include_graphics("images/4-variable.png")
```


<div class = "warning">
🎈 **Увага**: У школі, на уроках математики `x = 1` інтерпретувалось як рівняння *x дорівнює 1*. У програмуванні, цей запис має інше значення: ми присвоїли ім'я `x` до об'єкта `1` або іншими словами створили змінну яка вказує на `1`.
</div>

Спочатку, давайте подивимось що саме міститься в змінній `free_space`. Для цього ми можемо просто зазначити її ім'я в клітинці для коду та виконати її. Python роздрукує значення цієї змінної:

```{python}
free_space
```

Окей, їдемо далі. Коли змінна створена, ми можемо використовувати її в наших розрахунках 🧮. Давайте порахуємо кількість фотографій, які ми можемо зробити, коли в нас залишилось `4.5` вільної пам'яті. Тобто замість `4.5`, ми зазначимо нашу змінну `free_space`:

```{python}
free_space * 1000 / 3
```

Та кількість вільної пам'яті у відсотках:

```{python}
free_space / 64 * 100
```

А тепер ми змінимо значення змінної на `4` гігабайти і знову запустимо розрахунки. Тут важливо звернути увагу, що розрахунки використовуються нове значення `4`:

```{python}
free_space = 4
```

Давайте спочатку перевіримо, що `free_space` все ж таки містить значення `4`:

```{python}
free_space
```

Кількість фотографій, коли у нас `4` гігабайти вільної пам'яті буде:

```{python}
free_space * 1000 / 3
```

А вільна пам'ять в процентах буде:

```{python}
free_space / 64 * 100
```

## ➕ Рахуємо за допомогою  операторів

`r term("Оператори", "operators")` -- це спеціальні символи (наприклад, `*`, `/` тощо), які використовуються для операцій. Так, згода, це все одно, що сказати що вода мокра 🌊. Гарна новина у тому, що ми вже знайомі із дуже важливою частиною з них, математичними операторами, і вже вміємо їх використовувати. Математичні оператори збігаються з математичними символами:

* `+` -- `r term("додавання", "addition")`
* `-` -- `r term("віднімання", "subtraction")`
* `*` -- `r term("множення", "multiplication")`
* `/` -- `r term("ділення", "division")`

Це не все. Ще до математичних операторів відносять також: `r term("піднесення до степеня", "exponentiation")` (`**`), `r term("отримання неповної частки", "floor division")` (`//`) та `r term("остачі від ділення", "modulus")` (`%`). Якщо бути відвертим, то останні два оператори використовуються доволі рідко. Тому, якщо ви не пам'ятаєте або не знаєте 🤔 що таке не повна частка та остача -- не біда.

Давайте тепер трохи формалізуємо 👔 наші знання. Розглянемо множення двох чисел з нашого прикладу `free_space * 1000`. У цьому прикладі змінну `free_space` та значення `1000` ми будемо називати `r term("операндами", "operands")`. Тобто у випадку математичних операторів, операндами є все що стоїть ліворуч та стоїть праворуч від оператора. Математичні оператори мають дуже інтуїтивний синтаксис, який поділяється на три частини: перший операнд, безпосередньо сам оператор і другий операнд.

Йдемо далі. Послідовність з операндів та операторів називається `r term("виразом", "expression")`. Наприклад, `2 + 2` є виразом, і, наприклад `free_space / 64 * 100` також є складним, але все одно виразом. З цього слідує, що операндами можуть бути не тільки змінні та значення, а й інші вирази.

```{r, echo=FALSE, out.width="100%"}
knitr::include_graphics("images/4-operator.png")
```

Ми вже бачили багато прикладів з операторами, коли робили наші підрахунки. Тут ми тільки глянемо на новий оператор піднесення до степеня:

```{python}
3 ** 2
```

Крім математичних операторів існують ще інші, як, наприклад, оператори порівняння, логічні оператори тощо. Ми ще до них повернемось у Главі \@ref(ifelse).

До речі, у виразах з операторами, дужки відіграють таку ж роль, як і в математиці -- вони регулюють порядок виконання дій. Наприклад, вираз `2 + 3 * 4` буде мати зовсім інший результат ніж `(2 + 3) * 4`:

```{python}
2 + 3 * 4
```

Результат цього виразу не дорівнює значенню, яке поверне наступний вираз:

```{python}
(2 + 3) * 4
```

Ми також можемо записати результат виразу до змінної та потім використати її: 

```{python}
used_space = 64 - free_space
used_space / 64 * 100
```

## 🔡 Викликаємо функції

Повернемось до нашого прикладу з вільною пам'яттю смартфона. Кількість вільної пам'яті в `4` гігабайти відповідає `6.25%` від усієї доступної пам'яті телефона (дивись попередні розрахунки). Звичайно десяткова частина `0.25%` цього відсотка для нас не так суттєва і не відіграє важливу роль. Ми все одно округляємо в голові до `6%`, коли дивимось. Але ми не повинні цього робити в голові -- у Python існує механізм, який дозволяє комп'ютерові округлити ⭕️ це число. Цей механізм -- функція `round()`: 

```{python}
round(6.25)
```

Згодом ви побачите, що у програмуванні ми дуже часто виконуємо одні і ті самі завдання. Наприклад, завдання знайти максимальне значення серед чисел або знайти модуль числа виникають майже постійно. І щоб щоразу не писати код, який вирішує такі повторні завдання, ми можемо створити окрему невеличку програму, яку ми назвемо `r term("функцією", "function")`. Зазвичай у функцій є ім'я за яким ми можемо її викликати -- тобто виконати. Після імені функції треба написати дужки, між якими ми можемо зазначити `r term("аргументи", "arguments")` (якщо це передбачає функція). Аргументи дозволяють передавати інформацію у функцію, щоб функція могла виконати ту чи іншу дію, чи обчислення. Іншими словами, аргументи використовують для того, щоб вплинути на виконання функції.

```{r, echo=FALSE, out.width="100%"}
knitr::include_graphics("images/4-function.png")
```

До речі, аргументами функції можуть бути не тільки значення, а й змінні, вирази та виклики інших функцій. Повертаючись до прикладу про вільну пам'ять, ми можемо використати вираз `free_space / 64 * 100` безпосередньо як аргумент до функції:

```{python}
round(free_space / 64 * 100)
```

А ще ми можемо записати значення, яке поверне функція до змінної: 

```{python}
free_space_percentage = round(free_space / 64 * 100)
```

І потім використати, наприклад для того, щоб порахувати кількість зайнятої пам'яті: 

```{python}
100 - free_space_percentage
```

Ось приклад функції `max()`, яка повертає максимум серед зазначених чисел:

```{python}
max(3, 3.5, 10, -20)
```

А ось приклад функції `abs()`, яка знаходить `r term("модуль", "absolute value")` числа: 

```{python}
abs(-10)
```

Python охоплює 69 `r term("вбудованих", "built-in")` функцій. Коли ми кажемо що функція вбудована -- ми маємо на увазі, що ця функція є частиною Python. Інакше кажучи, ми можемо зразу ж нею користуватись. Активно користуватись ви будете скоріш всього не більш ніж 20 вбудованими функціями. Але на даному етапі це не повинно вас турбувати, ми вивчимо ці функції природним шляхом через практику, ніж будемо запам'ятовувати як вірші.

## 🪛 Користуємось методами та атрибутами об'єктів

Окрім функцій, є ще кілька корисних речей, які полегшать нам життя. По-перше, майже кожен об'єкт у Pyhon має так звані `r term("методи", "methods")`. Методи -- це функції які прикріплені до об'єктів. Вони використовуються щоб, наприклад, змінити цей об'єкт чи отримати якусь інформацію про цей об'єкт.

Давайте конкретніше! Спершу створимо змінну `half` (з англійської -- половина), яка вказуватиме на число `0.5`. Якщо ми захочемо представити це число у вигляді дробі 🍰, ми можемо скористатися методом `.as_integer_ratio()`, який поверне пару -- чисельник та знаменник. тобто `1` та `2`. Число $0.5$ у вигляді дробі має вигляд $\frac{1}{2}$, де $1$ -- це чисельник, а $2$ -- це знаменник.

Щоб використати цей метод, нам потрібно спочатку зазначити: ім'я нашої змінної `half`, крапку `.`, ім'я нашого методу `as_integer_ratio` і нарешті дужки `(` та `)`. Якщо метод передбачає використання аргументів, то їх треба зазначити між дужками. 

```{python}
half = 0.5
half.as_integer_ratio()
```

```{r, echo=FALSE, out.width="100%"}
knitr::include_graphics("images/4-method.png")
```

Наступна корисна штука називається `r term("атрибут", "attribute")`. Атрибут теж прикріплений до певного об'єкта та використовується для того, щоб отримати якусь інформацію про об'єкт. Наприклад, щоб отримати дійсну частину від нашої змінної `half`, ми можемо використати атрибут `.real`. Ми не будемо занурюватись у математику [комплексних чисел](https://uk.wikipedia.org/wiki/Комплексне_число) 😱, щоб зрозуміти що саме означає цей атрибут, нам тільки важливо зрозуміти як його отримати. А отримати значення атрибута дуже просто -- треба спочатку прописати ім'я змінної `half`, крапку `.`, і нарешті ім'я атрибуту `real`: 

```{python}
half.real
```

```{r, echo=FALSE, out.width="100%"}
knitr::include_graphics("images/4-attribute.png")
```

Ось і все, що нам потрібно зараз знати про методи та атрибути. До речі, у тексті цієї книги 📕 ми будемо позначати методи починаючи з крапки та закінчуючи дужками `.ім'я_метода()`, а атрибути -- просто починаючи з крапки `.ім'я_атрибуту`.

---

У цій главі ми дізнались для чого нам потрібні змінні і як з ними працювати. Більш того, ми навчились користуватись математичними операторами та викликати функції. Нарешті, ми познайомились з методами та атрибутами об'єктів. Не турбуйтесь, якщо якісь визначення ви не запам'ятали -- ми їх будемо ретельно пропрацьовувати в усіх наступних главах і незабаром ці терміни стануть для вас інтуїтивними.

---

<details id="vars_exercises">
<summary>🤸 Вправи</summary>

```{r, results='asis', message=FALSE, echo=FALSE}
question <- "1. Перетягніть атрибути та методи до їх відповідних кошиків 🧺:"

generate_bucket_quiz(
    question = question,
    elements = c(
        "<code>.numerator</code>" = "Атрибути",
        "<code>.conjugate()</code>" = "Методи",
        "<code>.denominator</code>" = "Атрибути",
        "<code>.bit_count()</code>" = "Методи"
    ),
    buckets = c("Атрибути", "Методи")
)
```

```{r, results='asis', message=FALSE, echo=FALSE}
question <- paste(
    "2. Як називаються <code>oranges</code> та <code>34</code> у",
    "виразі <code>34 / oranges</code> 🍊?"
)

generate_options_quiz(
    question = question,
    answers = c(
        "Змінними" = FALSE,
        "Операторами" = FALSE, 
        "Операндами" = TRUE
    ),
    allow_multiple_answers = FALSE
)
```

```{r, results='asis', message=FALSE, echo=FALSE}
question <- "3. Що поверне наступний код 🤓?"

between <- "<pre><code>quarter = 0.25
quarter.as_integer_ratio()
</code></pre>"

generate_options_quiz(
    question = question, 
    between = between,
    answers = c(
        "<code>(2, 8)</code>" = FALSE,
        "<code>(1, 4)</code>" = TRUE,
        "<code>1/4</code>" = FALSE,
        "<code>True</code>" = FALSE
    ),
    allow_multiple_answers = FALSE
)
```

```{r, results='asis', message=FALSE, echo=FALSE}
generate_short_quiz(
    question = "4. Запиши код, який поверне модуль числа <code>3.14</code> 🔪:",
    correct_answer = "abs(3.14)"
)
```

```{r, results='asis', message=FALSE, echo=FALSE}
question <- '5. Чому наступний код не працює 🤔?'

between <- "<pre><code>price_in_eur = 8
price_in_eur * eur_to_uah
eur_to_uah = 40.31
</code></pre>
"

generate_options_quiz(
    question = question, 
    between = between,
    answers = c(
        "Ми використовуємо оператор <code>*</code> замість оператора <code>/</code>" = FALSE,
        "Ми використовуємо змінну <code>eur_to_uah</code> до того, як її створити" = TRUE,
        "Python не вміє вирішувати рівняння" = FALSE
    ),
    allow_multiple_answers = FALSE
)
```

```{r, results='asis', message=FALSE, echo=FALSE}
question <- paste(
    "6. Враховуючи що функція <code>max()</code> повертає максимальне число",
    "серед зазначенних, то фукнція <code>min()</code> 💬..."
)

generate_options_quiz(
    question = question,
    answers = c(
        "...округлить числа!" = FALSE,
        "...поверне мінімальне!" = TRUE,
        "...поверне модуль числа!" = FALSE,
        "...поверне числа з протилежним знаком!" = FALSE
    ),
    allow_multiple_answers = FALSE
)
```

```{r, results='asis', message=FALSE, echo=FALSE}
question <- paste(
    "7. Перетягніть елементи коду, щоб у результаті отримати <code>4</code>:"
)

generate_arrange_quiz(
    question = question,
    elements = c(
        "<code>20</code>" = 1,
        "<code>1</code>" = 5,
        "<code>-</code>" = 4,
        "<code>/</code>" = 2,
        "<code>4</code>" = 3
    )
)
```

```{r, results='asis', message=FALSE, echo=FALSE}
question <- paste(
    '8. Наступний код повинен підрахувати площу',
    'круга 🟣 з радіусом <code>5</code>:'
)

between <- "<pre><code>______ = 5
pi = 3.14
pi * radius ^ 2
</code></pre>
<div class='question'>Що треба зазначити замість <code>______</code>, щоб код запрацював?</div>
"

generate_options_quiz(
    question = question, 
    between = between,
    answers = c(
        "<code>circle</code>" = FALSE,
        "<code>radius</code>" = TRUE,
        "<code>r</code>" = FALSE,
        "<code>diameter</code>" = FALSE
    ),
    allow_multiple_answers = FALSE
)
```

</details>

<!-- Подумай, може є сенс поміняти місцями оператори та функції, тоді вирішиться проблема з виразами -->
<!-- Подумай, може є сенс додати про методи???-->